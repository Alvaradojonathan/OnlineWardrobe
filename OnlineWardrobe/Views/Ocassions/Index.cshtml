@model IEnumerable<OnlineWardrobe.Models.Ocassion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ocassion1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ocassion1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OcassionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OcassionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OcassionID })
        </td>
    </tr>
}

</table>
